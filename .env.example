# Frontend app environment variables (production template)
# Copy to .env.local for local dev or to your deployment environment settings.

# Public frontend base URL (used for redirects, callback links, and client-side absolute URLs).
NEXT_PUBLIC_BASE_URL=https://app.example.com

# Server-side URLs for microservice proxies (never expose as NEXT_PUBLIC).
# Identity service: OTP, session, MFA, refresh tokens.
IDENTITY_SERVICE_URL=https://identity.example.com

# Core-platform service: jobs, calendar, companies, workers, admin APIs.
CORE_PLATFORM_SERVICE_URL=https://core.example.com

# Proxy timeout configuration (milliseconds).
# Requests that exceed this duration are terminated with 408 Request Timeout.
CORE_PLATFORM_PROXY_TIMEOUT_MS=10000

# Public Supabase values used by lib/supabase/client.browser.ts.
# These are safe to expose in the browser.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-supabase-anon-key

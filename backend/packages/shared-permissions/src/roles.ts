// Canonical user roles for the platform.
// admin: platform operator
// recruiter: company user who posts jobs
// trade: worker who applies for jobs

export enum UserRole {
  ADMIN = "admin",
  RECRUITER = "recruiter",
  TRADE = "trade",
}

import TicketForm from "@/components/trade_dashboard/support/TicketForm";

export default function TradeSupportNewPage() {
  // Page-level composition only
  return (
    <main className="min-h-screen bg-slate-50 p-6">
      <TicketForm />
    </main>
  );
}
